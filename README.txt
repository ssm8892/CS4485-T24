How to run the website:

- Start up WAMP Server (Linked to mySQL DB)
- Compile the SQL code in the "db.sql" file
- Find your MySQL Password and insert it in line 40 in the "server.js" file
- Open terminal on code editor with this repo and type "npm run start"
- Open your browser and go to "http://localhost:3000/"

